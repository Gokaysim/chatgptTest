Before run make sure run below command:
npm install

After installation you may run express server with:
npm run dev

You may also use other scripts to build, test, eslint checks, and running prod version
